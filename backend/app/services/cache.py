# from flask_caching import Cache

# cache = Cache()

# def init_cache(app):
#     """Initialize cache with Flask app."""
#     cache.init_app(app, config={"CACHE_TYPE": app.config["CACHE_TYPE"], "CACHE_REDIS_URL": app.config["CACHE_REDIS_URL"]})
